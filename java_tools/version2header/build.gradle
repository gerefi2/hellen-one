plugins {
    id 'java-library'
}

defaultTasks 'jar'

jar {
    destinationDirectory = file( '$rootDir/../..' )
    manifest {
        attributes(
            'Main-Class': 'com.gerefi.Version2Header'
        )
    }
}