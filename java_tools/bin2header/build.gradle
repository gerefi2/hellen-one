plugins {
    id 'java-library'
}

defaultTasks 'jar'

apply from: '../../android/dependencies.gradle'

jar {
    destinationDirectory = file( '$rootDir/../..' )
    manifest {
        attributes(
            'Main-Class': 'gerefi.Bin2Header'
        )
    }
}